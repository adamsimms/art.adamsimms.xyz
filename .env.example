# Local-only. Copy to `.env` or export in your shell.
# Mapbox public token (pk.*). Also add art.adamsimms.xyz to the token URL allowlist.
PUBLIC_MAPBOX_TOKEN=

# Inverted-circle site cursor. Off unless set to 1 or true.
# PUBLIC_SITE_CURSOR=1
